<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#1A2421"
    android:orientation="vertical"
    android:padding="12dp">

    <!-- 第一行：高度锁定 -->
    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="26dp">

        <LinearLayout
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_alignParentStart="true"
            android:layout_toStartOf="@+id/widget_increment"
            android:gravity="center_vertical"
            android:orientation="horizontal">

            <ImageView
                android:id="@+id/widget_circle"
                android:layout_width="6dp"
                android:layout_height="6dp"
                android:layout_marginEnd="6dp"
                android:src="@drawable/circle_shape" />

            <TextView
                android:id="@+id/widget_slogan"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:ellipsize="end"
                android:singleLine="true"
                android:textColor="#00FF88"
                android:textSize="12sp"
                android:textStyle="bold" />
        </LinearLayout>

        <TextView
            android:id="@+id/widget_increment"
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:layout_alignParentEnd="true"
            android:gravity="center_vertical"
            android:textColor="#FFA500"
            android:textSize="13sp"
            android:textStyle="bold" />
    </RelativeLayout>

    <!-- 第二行：主收益。关键：includeFontPadding=false 且 增加 marginBottom -->
    <TextView
        android:id="@+id/widget_earnings"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:gravity="center_vertical"
        android:includeFontPadding="false"
        android:layout_marginBottom="4dp"
        android:textColor="#00FF88"
        android:textSize="26sp"
        android:textStyle="bold" />

    <!-- 第三行：进度条（滚动条） -->
    <ProgressBar
        android:id="@+id/widget_progress"
        style="?android:attr/progressBarStyleHorizontal"
        android:layout_width="match_parent"
        android:layout_height="4dp"
        android:max="1000"
        android:progressTint="#00FF88"
        android:progressBackgroundTint="#3300FF88" />

</LinearLayout>
